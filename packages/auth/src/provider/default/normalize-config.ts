import { RawConfig } from '../interface';

// TODO: create normalized type
export type Config = RawConfig;

// TODO: perform validation & ensure correct signature
export function normalizeConfig(config: RawConfig): Config {
	return config;
}
